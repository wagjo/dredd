;; this is an example file, copy it into local_settings.clj and customize it

(ns dredd.local-settings
  "This namespace stores all local settings for the application")

;; settings for LDAP authentication
(def ldap
  {:host "localhost"
   :port 636
   :admin-dn "uid=admin-user-name,ou=admin,dc=example,dc=com"
   :admin-pass "admin-password"
   :base-dn "dc=example,dc=com" ;; where to search for users
   :user-login "uid"}) ;; how user login is interpreted in LDAP entry

(def neo4j
  {:path "./neo-db"})
